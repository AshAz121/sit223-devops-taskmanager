global:
  resolve_timeout: 15s

route:
  receiver: local-webhook
  group_wait: 5s
  group_interval: 5s
  repeat_interval: 1h

receivers:
  - name: local-webhook
    webhook_configs:
      - url: http://sit223-alert-receiver:5001/alerts
        send_resolved: true
